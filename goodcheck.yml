rules:
  - id: com.example.1
    pattern: id
    message:
    glob:
      - "**/*.rb"
      - "**/*.{yaml,yml}"
      - "public/**/*.html"
    fail:
      - Signup via Github
    pass:
      - Signup via GitHub
