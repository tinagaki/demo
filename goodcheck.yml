rules:
  - id: com.example.1
    pattern: DemoController
    message: TESTTESTTEST
    glob:
      - "**/*.java"
      - "**/*.{yaml,yml}"
      - "public/**/*.html"
    fail:
      - Signup via Github
    pass:
      - Signup via GitHub
